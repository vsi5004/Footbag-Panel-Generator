class Panel{
    constructor(){
        this.bite = bite
        this.stitches = stitches
        this.stitch_len = stitch_length
        this.hole_dia = hole_diameter
        this.side_len = side_len
        this.sides = sides
        this.skip_sides = skip_sides
        this.angle = 2 * math.pi / len(sides)
        this.vertices = self.get_vertices()
        const [this.x_coords, self.y_coords] = self.get_xy_coord_lists()
        self.rotate_point = self.get_rotate_point()
    }
}